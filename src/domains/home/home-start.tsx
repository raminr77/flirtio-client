import { HOME_MENU } from "./constants";

export function HomeStart() {
  return (
    <section id={HOME_MENU.START} className='w-full h-screen flex flex-col items-center justify-center'>
      <div>Start</div>
    </section>
  )
}
